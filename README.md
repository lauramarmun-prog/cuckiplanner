# Cucki Planner MCP

Backend MCP separado para Cucki Planner.

## Run local

```bash
uvicorn cucki_main:app --reload --port 8000
```

## Render start command

```bash
uvicorn cucki_main:app --host 0.0.0.0 --port $PORT
```
